name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            name: x86_64-linux
            triplet: x64-linux
            archive: tar.gz
          - os: windows-latest
            name: win64
            triplet: x64-windows-static
            archive: zip
          - os: macos-14
            name: arm64-darwin
            triplet: arm64-osx
            archive: tar.gz

    env:
      VCPKG_DEFAULT_TRIPLET: ${{ matrix.triplet }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        shell: bash
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build pkg-config \
            libssl-dev libsecp256k1-dev libleveldb-dev libsnappy-dev

      - name: Install system dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install cmake ninja pkg-config openssl@3 leveldb secp256k1 snappy

      - name: Setup MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install Ninja (Windows)
        if: runner.os == 'Windows'
        run: choco install ninja -y

      - name: Setup vcpkg (Windows)
        if: runner.os == 'Windows'
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: '84bab45d415d22042bd0b9081aea57f362da3f35'

      - name: Cache vcpkg packages (Windows)
        if: runner.os == 'Windows'
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/vcpkg/installed
          key: vcpkg-${{ matrix.triplet }}-${{ hashFiles('vcpkg.json') }}
          restore-keys: |
            vcpkg-${{ matrix.triplet }}-

      - name: Configure (Windows)
        if: runner.os == 'Windows'
        run: >
          cmake -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_CXX_STANDARD=20
          -DVCPKG_TARGET_TRIPLET=x64-windows-static
          -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded
          -DCMAKE_TOOLCHAIN_FILE=${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake

      - name: Configure (Linux)
        if: runner.os == 'Linux'
        run: >
          cmake -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_CXX_STANDARD=20

      - name: Configure (macOS)
        if: runner.os == 'macOS'
        run: >
          cmake -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_CXX_STANDARD=20
          -DOPENSSL_ROOT_DIR=$(brew --prefix openssl@3)
          -DCMAKE_PREFIX_PATH=$(brew --prefix)
          -DCMAKE_EXE_LINKER_FLAGS="-L$(brew --prefix)/lib"

      - name: Build
        run: cmake --build build --parallel

      - name: Package (Linux / macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          PKG_NAME="resonancenet-${VERSION}-${{ matrix.name }}"
          mkdir -p "${PKG_NAME}"
          for bin in rnetd rnet-cli rnet-tx rnet-wallet-tool rnet-util rnet-miner rnet-prepare-dataset rnet-train-test; do
            cp "build/${bin}" "${PKG_NAME}/" 2>/dev/null || true
          done
          cp COPYING README.md "${PKG_NAME}/"
          tar czf "${PKG_NAME}.tar.gz" "${PKG_NAME}"
          echo "ARCHIVE=${PKG_NAME}.tar.gz" >> "$GITHUB_ENV"

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          PKG_NAME="resonancenet-${VERSION}-${{ matrix.name }}"
          mkdir -p "${PKG_NAME}"
          for bin in rnetd rnet-cli rnet-tx rnet-wallet-tool rnet-util rnet-miner rnet-prepare-dataset rnet-train-test; do
            cp "build/${bin}.exe" "${PKG_NAME}/" 2>/dev/null || true
          done
          # Include OpenSSL DLLs so users don't get missing DLL errors.
          cp build/*.dll "${PKG_NAME}/" 2>/dev/null || true
          cp "$VCPKG_INSTALLED_DIR/x64-windows-static/bin/"*.dll "${PKG_NAME}/" 2>/dev/null || true
          find "${{ github.workspace }}/vcpkg/installed" -name "libcrypto*.dll" -exec cp {} "${PKG_NAME}/" \; 2>/dev/null || true
          find "${{ github.workspace }}/vcpkg/installed" -name "libssl*.dll" -exec cp {} "${PKG_NAME}/" \; 2>/dev/null || true
          cp COPYING README.md "${PKG_NAME}/"
          7z a "${PKG_NAME}.zip" "${PKG_NAME}"
          echo "ARCHIVE=${PKG_NAME}.zip" >> "$GITHUB_ENV"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: ${{ env.ARCHIVE }}
          retention-days: 1

  release:
    name: Create GitHub Release
    needs: build
    if: always() && !cancelled()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create source archive
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          git archive --format=tar.gz --prefix="resonancenet-${VERSION}/" \
            -o "artifacts/resonancenet-${VERSION}-source.tar.gz" HEAD

      - name: List release assets
        run: ls -lh artifacts/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ResonanceNet ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') }}
          files: artifacts/*
