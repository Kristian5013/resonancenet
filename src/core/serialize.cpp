#include "core/serialize.h"

// The serialization framework is entirely header-based templates.
// This translation unit ensures the header compiles cleanly.

namespace rnet::core {

// Intentionally empty — all serialization logic is in templates.

}  // namespace rnet::core
