#include "core/signal.h"

// Signal<Args...> is fully implemented in the header as a template.
// This translation unit ensures the header compiles cleanly.

namespace rnet::core {

// Intentionally empty — all Signal logic is in the header template.

}  // namespace rnet::core
