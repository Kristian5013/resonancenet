#include "consensus/validation.h"

// ValidationState is header-only; this file exists for CMake consistency
// and future expansion (e.g., validation statistics, logging hooks).

namespace rnet::consensus {

// Intentionally empty — all methods are inline in the header.

}  // namespace rnet::consensus
